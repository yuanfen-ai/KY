/**
 * 运行时配置文件
 * 
 * 本文件在构建后可由部署人员直接修改，无需重新构建。
 * 修改后刷新浏览器即可生效。
 * 
 * 配置优先级：
 * 1. 本文件 (window.__APP_CONFIG__)
 * 2. 构建时环境变量 (VITE_*)
 * 3. 代码中的默认值
 */
window.__APP_CONFIG__ = {
  // 地图服务地址
  VITE_MAP_TARGET: 'http://1.14.100.199:8888',
  
  // WebSocket服务地址
  VITE_WS_TARGET: 'ws://1.14.100.199:8050',
};
